% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/document.R
\name{document}
\alias{document}
\title{Document a Decision From APSIM NG Outputs}
\usage{
document(
  data,
  decision,
  context = list(),
  criteria = list(),
  options = list(),
  simplify = FALSE,
  ...
)
}
\arguments{
\item{data}{A data frame or tibble containing APSIM Next Generation
simulation outputs.}

\item{decision}{A single string identifying which decision workflow to run,
such as \code{"cultivar"}.}

\item{context}{A named list of contextual inputs supplied to the selected
decision workflow.}

\item{criteria}{A named list of decision criteria or thresholds used by the
selected decision workflow.}

\item{options}{A named list of additional options that control reporting or
documentation behaviour in the selected decision workflow.}

\item{simplify}{A boolean flag indicating whether to simplify the returned
document into a character vector of lines. If \code{FALSE}, the full structured
document is returned.}

\item{...}{Additional arguments passed through to the domain-specific
decision documenter.}
}
\value{
A list with class \code{"rapsimng_decide_report"} containing four named
components:
\itemize{
\item \code{meta}: metadata describing the decision context and execution.
\item \code{metrics}: summary metrics produced by the decision workflow.
\item \code{tables}: tabular outputs ready for reporting.
\item \code{figures}: figure objects, specifications, or references for visual output.
}
}
\description{
\code{document()} is a high-level orchestration wrapper for decision
workflows built on APSIM Next Generation outputs. It validates inputs,
dispatches to a domain-specific decision package, and standardises the
returned result into a common report structure. It does not implement the
decision logic itself.
}
\details{
This package separates responsibilities across the wider workflow:
\itemize{
\item \code{rapsimng} is responsible for running or preparing APSIM Next Generation
simulations and outputs.
\item \verb{rapsimng.decide.*} packages implement domain-specific decision logic,
such as cultivar suitability assessment.
\item downstream analysis or interpretation layers, including packages such as
\code{agrillm}, can consume the standardised report returned here.
}

\code{rapsimng.decide} sits between these layers and provides a consistent entry
point and output contract for decision-oriented reporting.
}
